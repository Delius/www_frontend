<!DOCTYPE html>
<html>
<head>
  <title>Bigera</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= favicon_link_tag %>
  <%= csrf_meta_tags %>
</head>
<body>
<div id="container">
	<header>
		<%= link_to content_tag('canvas', 'Bigera', id: 'logocanvas'), root_url %>
		<nav>
			<ul>
				<% nav2_items.each do |nav_item| %>
				<%= display_nav2_item(nav_item[:displaytext], nav_item[:controllername], nav_item[:linkurl]) %>
				<% end %>
				<% if user_signed_in? %>
				<% nav_items.each do |nav_item| %>
					<%= display_nav_item(nav_item[:displaytext], nav_item[:controllername], nav_item[:linkurl]) %>
				<% end %>
				<% end %>
			</ul>
		</nav>
	</header>
	<section id="main">
		<article>
			<% [:notice, :alert].each do |level| %>
				<% if flash[level] %>
					<div class="flash <%= level %>"><%= flash[level] %></div>
				<% end %>
		    <% end %>
			<%= yield %>
		</article>
		<aside>
			<%= user_logged_in_msg %>
			<%= yield :aside %>
		</aside>
	</section>
	<footer>
		<div>
		<p>Bigera ltd <em> 2016</em>  </div>
		18 Brisbane Street <br><em>  Greenock</em><br>
		PA16 8LP</p>
	</footer>
</div>
</body>
</html>
