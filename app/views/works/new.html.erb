<h1>New Work</h1>
<%= form_for(@work) do |f| %>
<div>
	<%= f.label :hours %>
	<%= f.select (:hours, [1,2,3,4,5,6,7,8], prompt: true %>
	
</div>
	<div>
		<%= f.submit "Create Work" %>
	</div>
	<% end %>

	<% content_for :aside do %>
	<%= render 'sidebar' %>
	<% end %>