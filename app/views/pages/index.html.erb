<h1>Pages</h1>

<p>Here are all of the pages:</p>

<table>
	<tbody>
		<tr>
			<th>Page</th>
		</tr>
		<%= render partial: "page", collection: @pages %>
			</tbody>
</table>

<% content_for :aside do %>
	<%= render 'sidebar' %>
<% end %>