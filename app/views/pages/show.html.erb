<h1><%= @page.title%></h1>

<p><%= link_to 'Edit Page', edit_page_path(@page) %></p>


	<table>
		<tbody>
			<tr>
				<th>Example</th>
				<th></th>
			</tr>
			<%= render partial: "examples/example", collection: @page.examples.all %>
		</tbody>
	</table>

	<p>No examples</p>


<% content_for :aside do %>
	<%= render 'sidebar' %>
<% end %>
